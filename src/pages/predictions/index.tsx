import React from 'react';

import Layout from '@/components/layout/Layout';

const Predictions = () => {
  return (
    <Layout>
      <div>Predictions</div>
    </Layout>
  );
};

export default Predictions;
